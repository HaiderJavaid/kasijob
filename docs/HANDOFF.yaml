date: 2026-05-31
project:
  local_path: /Users/kinghaider/Desktop/Coding/projects/kasi-job
  github_repo: https://github.com/HaiderJavaid/kasijob.git
status:
  active_ui: app/leaderboard/page.js
  positioning: jobs-first freelance marketplace demo with landing-page preview of the logged-in jobs board
  current_risks:
    - Homepage preview is visually QA'd via local screenshots, but still needs manual browser QA on a real phone and desktop browser before release.
    - Lint still reports pre-existing warnings outside the leaderboard fix.
    - Leaderboard now reads all user docs client-side, which is acceptable for the current small demo but should move server-side or be paginated before production scale.
deployment:
  host_target: unknown
  branch: main
  latest_commit: 5e727b8
  config_file: null
  pushed: true
  pr_url: null
verification:
  last_run:
    - npm test
    - npm run lint
    - npm run build
  result: pass-with-existing-warnings
  screenshots:
    - output/playwright/homepage.png
    - output/playwright/homepage-preview-desktop-v2.png
    - output/playwright/homepage-preview-mobile-collapsed-section-v2.png
dirty_worktree:
  untracked:
    - output/playwright/homepage-preview-mobile-collapsed-section-v2.png
done:
  - Fixed leaderboard users disappearing by removing the `emailVerified:false` filter.
  - Added pure leaderboard sorting/capping logic with tests.
  - Leaderboard now includes users with missing balances as RM0 and ranks top 10 by wallet balance.
next:
  - Manually QA homepage preview interactions and spacing on real mobile and desktop browsers.
  - Decide whether the generated preview screenshots in output/playwright should remain tracked artifacts or be cleaned before commit.
  - Manually QA leaderboard against the real Firebase project.
  - Continue the messaging notification work after homepage QA is accepted.
